% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cgf-stratified-mv.R
\name{fastperm_linear_cgf_mv}
\alias{fastperm_linear_cgf_mv}
\title{Joint CGF of a vector of linear statistics under within-stratum permutation}
\usage{
fastperm_linear_cgf_mv(scores, treatment, strata)
}
\arguments{
\item{scores}{numeric matrix of per-unit scores (units x representations).}

\item{treatment}{0/1 vector; its per-stratum sums fix the treated counts.}

\item{strata}{factor or vector of stratum labels.}
}
\value{
an object of class \code{fastperm_cgf_mv}: a list with \code{cgf} (a closure of
the length-d vector \code{s}), the exact \code{mean} vector and \code{cov} matrix, and \code{d}.
}
\description{
Builds the joint cumulant generating function \code{K_T(s)} of the vector statistic
\verb{T_c = sum_i treatment_i * score_\{i,c\}}, referred to the permutation null that
fixes each stratum's treated count. \code{K_T(s)} is evaluated by projecting the
score matrix onto \code{s} and applying the univariate elementary-symmetric CGF, so
the cost is that of one 1-D CGF per evaluation. The exact permutation mean
vector and covariance are returned in closed form; \verb{grad K_T(0)} and
\verb{Hess K_T(0)} reproduce them.
}
