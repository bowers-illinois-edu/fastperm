% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spa-linear.R
\name{fastperm_spa_linear}
\alias{fastperm_spa_linear}
\title{Saddlepoint permutation p-value for a linear statistic}
\usage{
fastperm_spa_linear(
  score,
  treatment,
  strata,
  alternative = c("two.sided", "greater", "less")
)
}
\arguments{
\item{score}{numeric vector of per-unit scores \code{c_i} (for a centred
representation, the within-stratum-centred values).}

\item{treatment}{0/1 vector; its per-stratum sums give the treated counts that
define the permutation null.}

\item{strata}{factor or vector of stratum labels, the same length as \code{score}.}

\item{alternative}{\code{"two.sided"} (default), \code{"greater"} (\code{P(T >= t_obs)}), or
\code{"less"} (\code{P(T <= t_obs)}).}
}
\value{
a list with \code{p.value}, the observed \code{statistic}, the permutation
\code{mean} and \code{sd}, the \code{alternative}, and the underlying \code{cgf} object.
}
\description{
Computes the within-stratum permutation p-value of the linear statistic
\verb{T = sum_i treatment_i * score_i} by saddlepoint approximation -- closed-form,
with no re-randomization. The permutation null fixes each stratum's treated
count.
}
\examples{
score <- c(1, 2, 3, 4)
fastperm_spa_linear(score, c(0, 1, 0, 1), rep(1, 4), alternative = "greater")$p.value
}
