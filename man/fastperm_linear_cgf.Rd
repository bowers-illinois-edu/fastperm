% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cgf-stratified.R
\name{fastperm_linear_cgf}
\alias{fastperm_linear_cgf}
\title{Exact CGF of a linear statistic under the within-stratum permutation null}
\usage{
fastperm_linear_cgf(score, treatment, strata)
}
\arguments{
\item{score}{numeric vector of per-unit scores \code{c_i} (for a centred
representation, the within-stratum-centred values).}

\item{treatment}{0/1 vector; its per-stratum sums give the treated counts that
define the permutation null.}

\item{strata}{factor or vector of stratum labels, the same length as \code{score}.}
}
\value{
an object of class \code{fastperm_cgf}: a list with \code{cgf}, \code{d1}, \code{d2}
(closures in \code{theta}), and the scalars \code{mean}, \code{variance}, \code{supmin},
\code{supmax}.
}
\description{
Builds the cumulant generating function of the linear statistic
\verb{T = sum_i treatment_i * score_i} referred to the permutation null that holds
each stratum's treated count fixed (a simple random sample of treated units
within each stratum). The returned object carries the exact permutation mean,
variance, and support of \code{T}, plus closures for \code{K(theta)} and its first two
derivatives, which \code{\link[=saddlepoint_tail]{saddlepoint_tail()}} inverts into a tail probability.
}
\examples{
score <- c(1, 2, 3, 4)
cg <- fastperm_linear_cgf(score, c(0, 1, 0, 1), rep(1, 4))
cg$mean
}
