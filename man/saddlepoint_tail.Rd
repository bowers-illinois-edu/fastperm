% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/saddlepoint.R
\name{saddlepoint_tail}
\alias{saddlepoint_tail}
\title{Saddlepoint tail probability of a linear statistic}
\usage{
saddlepoint_tail(cgf, t, lower.tail = FALSE, log.p = FALSE)
}
\arguments{
\item{cgf}{a \code{fastperm_cgf} object from \code{\link[=fastperm_linear_cgf]{fastperm_linear_cgf()}}.}

\item{t}{the value at which to evaluate the tail.}

\item{lower.tail}{if \code{FALSE} (default) return \code{P(T >= t)}; if \code{TRUE}, \code{P(T <= t)}.}

\item{log.p}{if \code{TRUE}, return the natural log of the probability.}
}
\value{
a probability in \verb{[0, 1]} (or its log if \code{log.p = TRUE}).
}
\description{
Inverts a \code{\link[=fastperm_linear_cgf]{fastperm_linear_cgf()}} object into a one-sided permutation tail
probability \code{P(T >= t)} (default) or \code{P(T <= t)}, using the Lugannani-Rice
saddlepoint approximation. Probabilities outside the statistic's support are
returned exactly (0 or 1). The small tail is computed in log space, so
\code{log.p = TRUE} returns accurate values far past the reach of simulation.
}
\examples{
cg <- fastperm_linear_cgf(c(1, 2, 3, 4), c(0, 1, 0, 1), rep(1, 4))
saddlepoint_tail(cg, 6)              # P(T >= 6)
}
